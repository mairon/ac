<nav>
  <div class="breadCrumb module">
      <ul>
          <li>
              Cadastros
          </li>
          <li>&#187;</li>
          <li>
              Cidades
          </li>
      </ul>
  </div>
</nav>
<style type="text/css">
  form{ float: left;}
  select{  width: 250px; margin:0px !important; }
</style>

<div style="color:red;">
<%= flash[:error] %>
</div>
  <div class="search_breadCrumb search">
    <div class="bts_grid">
    </div>
    <div class="row-fluid">
      <div class="span12">            
        <div data-fileupload="file" class="fileupload fileupload-new"><input type="hidden" />
          <div class="input-append">
            <%= link_to 'Novo', new_admin_city_path, :class => "btn" %>
          </div>
        </div>
        <table class="table table-bordered dTableR" id="dt_a">
            <thead>
              <tr>
                <th width="30">Codigo</th>
                <th>País</th>
                <th>Estado</th>
                <th>Cidade</th>
                <th></th>
              </tr>
            </thead>
            <tbody>
              <% @cities.each do |c| %>
                <tr>
                  <td><%= c.id.to_s.rjust(6,'0') %></td>
                  <td><%= c.country.name %></td>
                  <td><%= c.state.name %></td>
                  <td><%= c.name %></td>
                  <td>
                    <%= link_to '', edit_admin_city_path(c), class: "icon-pencil" %>
                    <%= link_to '', [:admin, c], method: :delete, data: { confirm: 'Deseja realmente remover o registro?' }, class: "icon-trash" %>
                  </td>
                </tr>
              <% end %>
            </tbody>
        </table>
      </div>
    </div>
  </div>
